package CustomThreads;

// corresponds to method 2 of "Creating Threads in Java" notes
public class EnemySpawnThread extends Thread {
    // constructor
    public EnemySpawnThread(String threadName) {
        super(threadName);
    }

    @Override
    public void run() {
        // insert thread task
    }
}
